type Props = {
  children: React.ReactNode;
  className?: string;
  onClick?: () => void;
};

const DiscoverButton = () => {
  return <button>DiscoverButton</button>;
};

export default DiscoverButton;
